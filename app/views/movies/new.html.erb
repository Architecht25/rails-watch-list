<h1>Creation d'une nouvelle liste</h1>

<div class="container mt-5">
  <h1>🎬 Nouvelle Liste de Films</h1>

  <%= simple_form_for @movie do |f| %>
    <div class="form-group">
      <%= f.input :title, label: 'Titre du Film', required: true %>
    </div>

    <div class="form-group">
      <%= f.input :overview, label: 'Résumé', as: :text, required: true %>
    </div>

    <div class="form-group">
      <%= f.input :poster_url, label: 'URL de l\'Affiche', required: true %>
    </div>
    <div class="form-group">
      <%= f.input :rating, label: 'Note', as: :select, collection: (1..10).to_a, include_blank: true, required: true %>
    </div>
    <div class="form-group">
      <%= f.button :submit, 'Créer la Liste', class: 'btn btn-success' %>
    </div>
  <% end %>
  <%= link_to '⬅ Retour à la liste des films', movies_path, class: 'btn btn-primary mt-3' %>
